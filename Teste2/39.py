def sum_of_differences(arr):
    if len(arr) <= 1:
        return 0
    else:
        ar = sorted(arr, reverse=True)
        for x in ar:
            for y in ar:
                
    return 

print(sum_of_differences([2, 1, 10]))